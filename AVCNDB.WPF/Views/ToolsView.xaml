<UserControl x:Class="AVCNDB.WPF.Views.ToolsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- En-tête -->
        <Border Grid.Row="0" Padding="24,16" Background="{DynamicResource MaterialDesignPaper}">
            <StackPanel>
                <TextBlock Text="Outils" 
                           Style="{StaticResource MaterialDesignHeadline4TextBlock}"
                           Foreground="{StaticResource PrimaryBrush}"/>
                <TextBlock Text="Outils utilitaires et fonctionnalités avancées" 
                           Style="{StaticResource MaterialDesignBody2TextBlock}"
                           Foreground="{DynamicResource MaterialDesignBodyLight}"
                           Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <!-- Contenu - Grille d'outils -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <WrapPanel Margin="24" Orientation="Horizontal">
                
                <!-- Outil: Export Excel -->
                <materialDesign:Card Width="280" Margin="8" Padding="16">
                    <StackPanel>
                        <materialDesign:PackIcon Kind="FileExcel" 
                                                 Width="48" Height="48" 
                                                 Foreground="#217346"
                                                 HorizontalAlignment="Center"/>
                        <TextBlock Text="Export Excel" 
                                   Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                   HorizontalAlignment="Center"
                                   Margin="0,12,0,4"/>
                        <TextBlock Text="Exporter les données vers un fichier Excel" 
                                   Style="{StaticResource MaterialDesignBody2TextBlock}"
                                   TextAlignment="Center"
                                   Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                        <Button Content="Exporter" 
                                Style="{StaticResource MaterialDesignOutlinedButton}"
                                Margin="0,16,0,0"
                                HorizontalAlignment="Center"/>
                    </StackPanel>
                </materialDesign:Card>

                <!-- Outil: Import Excel -->
                <materialDesign:Card Width="280" Margin="8" Padding="16">
                    <StackPanel>
                        <materialDesign:PackIcon Kind="FileImport" 
                                                 Width="48" Height="48" 
                                                 Foreground="#217346"
                                                 HorizontalAlignment="Center"/>
                        <TextBlock Text="Import Excel" 
                                   Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                   HorizontalAlignment="Center"
                                   Margin="0,12,0,4"/>
                        <TextBlock Text="Importer des données depuis un fichier Excel" 
                                   Style="{StaticResource MaterialDesignBody2TextBlock}"
                                   TextAlignment="Center"
                                   Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                        <Button Content="Importer" 
                                Style="{StaticResource MaterialDesignOutlinedButton}"
                                Margin="0,16,0,0"
                                HorizontalAlignment="Center"/>
                    </StackPanel>
                </materialDesign:Card>

                <!-- Outil: Rapport PDF -->
                <materialDesign:Card Width="280" Margin="8" Padding="16">
                    <StackPanel>
                        <materialDesign:PackIcon Kind="FilePdfBox" 
                                                 Width="48" Height="48" 
                                                 Foreground="#E53935"
                                                 HorizontalAlignment="Center"/>
                        <TextBlock Text="Rapport PDF" 
                                   Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                   HorizontalAlignment="Center"
                                   Margin="0,12,0,4"/>
                        <TextBlock Text="Générer un rapport PDF des médicaments" 
                                   Style="{StaticResource MaterialDesignBody2TextBlock}"
                                   TextAlignment="Center"
                                   Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                        <Button Content="Générer" 
                                Style="{StaticResource MaterialDesignOutlinedButton}"
                                Margin="0,16,0,0"
                                HorizontalAlignment="Center"/>
                    </StackPanel>
                </materialDesign:Card>

                <!-- Outil: Sauvegarde -->
                <materialDesign:Card Width="280" Margin="8" Padding="16">
                    <StackPanel>
                        <materialDesign:PackIcon Kind="DatabaseExport" 
                                                 Width="48" Height="48" 
                                                 Foreground="{StaticResource PrimaryBrush}"
                                                 HorizontalAlignment="Center"/>
                        <TextBlock Text="Sauvegarde" 
                                   Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                   HorizontalAlignment="Center"
                                   Margin="0,12,0,4"/>
                        <TextBlock Text="Créer une sauvegarde de la base de données" 
                                   Style="{StaticResource MaterialDesignBody2TextBlock}"
                                   TextAlignment="Center"
                                   Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                        <Button Content="Sauvegarder" 
                                Style="{StaticResource MaterialDesignOutlinedButton}"
                                Margin="0,16,0,0"
                                HorizontalAlignment="Center"/>
                    </StackPanel>
                </materialDesign:Card>

                <!-- Outil: Restauration -->
                <materialDesign:Card Width="280" Margin="8" Padding="16">
                    <StackPanel>
                        <materialDesign:PackIcon Kind="DatabaseImport" 
                                                 Width="48" Height="48" 
                                                 Foreground="#FF9800"
                                                 HorizontalAlignment="Center"/>
                        <TextBlock Text="Restauration" 
                                   Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                   HorizontalAlignment="Center"
                                   Margin="0,12,0,4"/>
                        <TextBlock Text="Restaurer depuis une sauvegarde" 
                                   Style="{StaticResource MaterialDesignBody2TextBlock}"
                                   TextAlignment="Center"
                                   Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                        <Button Content="Restaurer" 
                                Style="{StaticResource MaterialDesignOutlinedButton}"
                                Margin="0,16,0,0"
                                HorizontalAlignment="Center"/>
                    </StackPanel>
                </materialDesign:Card>

                <!-- Outil: Statistiques -->
                <materialDesign:Card Width="280" Margin="8" Padding="16">
                    <StackPanel>
                        <materialDesign:PackIcon Kind="ChartBar" 
                                                 Width="48" Height="48" 
                                                 Foreground="#9C27B0"
                                                 HorizontalAlignment="Center"/>
                        <TextBlock Text="Statistiques" 
                                   Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                   HorizontalAlignment="Center"
                                   Margin="0,12,0,4"/>
                        <TextBlock Text="Voir les statistiques de la base de données" 
                                   Style="{StaticResource MaterialDesignBody2TextBlock}"
                                   TextAlignment="Center"
                                   Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                        <Button Content="Voir" 
                                Style="{StaticResource MaterialDesignOutlinedButton}"
                                Margin="0,16,0,0"
                                HorizontalAlignment="Center"/>
                    </StackPanel>
                </materialDesign:Card>
                
            </WrapPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
