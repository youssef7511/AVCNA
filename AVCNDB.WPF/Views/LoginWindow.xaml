<Window x:Class="AVCNDB.WPF.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:vm="clr-namespace:AVCNDB.WPF.ViewModels"
        xmlns:helpers="clr-namespace:AVCNDB.WPF.Helpers"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:LoginViewModel}"
        Title="AVCNDB — Connexion"
        Width="420" Height="460"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        ShowInTaskbar="True">

    <Grid Margin="40,32,40,32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Logo + Titre -->
        <TextBlock Grid.Row="0"
                   Text="&#x1F3E5;"
                   FontSize="40"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,6"/>

        <TextBlock Grid.Row="1"
                   Text="AVCNDB — Connexion"
                   FontSize="20"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Foreground="Black"
                   Margin="0,0,0,4"/>

        <TextBlock Grid.Row="2"
                   Text="Base de données médicamenteuse"
                   FontSize="12"
                   HorizontalAlignment="Center"
                   Foreground="{DynamicResource MaterialDesignBodyLight}"
                   Margin="0,0,0,28"/>

        <!-- Identifiant -->
        <TextBox Grid.Row="3"
                 Style="{StaticResource MaterialDesignFilledTextBox}"
                 materialDesign:HintAssist.Hint="Identifiant"
                 Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                 Foreground="Black"
                 Margin="0,0,0,16"/>

        <!-- Mot de passe -->
        <PasswordBox Grid.Row="4"
                     Style="{StaticResource MaterialDesignFilledPasswordBox}"
                     materialDesign:HintAssist.Hint="Mot de passe"
                     helpers:PasswordBoxHelper.BindPassword="True"
                     helpers:PasswordBoxHelper.BoundPassword="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     Foreground="Black"
                     Margin="0,0,0,12"/>

        <!-- Se souvenir de moi -->
        <CheckBox Grid.Row="5"
                  Content="Se souvenir de moi sur ce poste"
                  IsChecked="{Binding RememberMe}"
                  Foreground="Black"
                  Margin="0,0,0,16"/>

        <!-- Bandeau d'erreur -->
        <Border Grid.Row="6"
                Background="#FFEBEE"
                CornerRadius="4"
                Padding="12,8"
                Margin="0,0,0,16"
                Visibility="{Binding HasLoginError, Converter={StaticResource BoolToVisibility}}">
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="AlertCircle"
                                         Width="18" Height="18"
                                         Foreground="#C62828"
                                         VerticalAlignment="Center"
                                         Margin="0,0,8,0"/>
                <TextBlock Text="{Binding LoginError}"
                           Foreground="#C62828"
                           FontSize="13"
                           TextWrapping="Wrap"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Boutons -->
        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Annuler"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Command="{Binding CancelCommand}"
                    Margin="0,0,10,0"
                    MinWidth="90"/>
            <Button Content="Se connecter"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Command="{Binding SignInCommand}"
                    MinWidth="120"/>
        </StackPanel>
    </Grid>
</Window>
