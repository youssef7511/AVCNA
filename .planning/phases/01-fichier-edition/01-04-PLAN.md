---
phase: 01-fichier-edition
plan: 04
type: execute
wave: 4
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - AVCNDB.WPF/Views/MovementsView.xaml
  - AVCNDB.WPF/Views/MovementsView.xaml.cs
autonomous: false
requirements: [EDIT-05]

must_haves:
  truths:
    - "User sees the page header 'Fichier d'édition' (not the old Mouvements placeholder)"
    - "User sees the import toolbar with a source type ComboBox and an Import button"
    - "User sees the display filter bar with all 13 filter options"
    - "User sees the DataGrid with correct columns: ☑ Lig. Id. Action Code-PCT Dénomination D.C.J Association Forme Tab. VEiC Labo. Prix-Réf Prix Remb. A.P. Désignation"
    - "Cells whose field is unknown (IsUnknown_Dci etc.) display value in blue bold via DataTrigger (D-03)"
    - "Rows with ActionFlag=AjouterNew show a blue 'A ajouter' badge in the Action column (D-04)"
    - "Right-clicking a row shows a ContextMenu with Substituer/Désaffecter/Marquer comme nouveau/Marquer pour la suppression/Ré-initialiser/Supprimer"
    - "User can verify the page renders without errors after running the app"
  artifacts:
    - path: "AVCNDB.WPF/Views/MovementsView.xaml"
      provides: "Full Fichier d'édition page UI"
      contains: "EditionFileViewModel, DataGrid, filter bar, context menu, DataTrigger blue cells"
    - path: "AVCNDB.WPF/Views/MovementsView.xaml.cs"
      provides: "Code-behind wiring EditionFileViewModel via constructor injection"
  key_links:
    - from: "AVCNDB.WPF/Views/MovementsView.xaml"
      to: "AVCNDB.WPF/ViewModels/EditionFileViewModel.cs"
      via: "d:DataContext DesignInstance + DI injection in code-behind"
      pattern: "EditionFileViewModel"
    - from: "MovementsView DataGrid"
      to: "EditionFileViewModel.Rows ObservableCollection"
      via: "ItemsSource binding"
      pattern: "ItemsSource.*Rows"
    - from: "DataGrid cell DataTrigger"
      to: "EditionRowViewModel.IsUnknown_Dci (etc.)"
      via: "DataTrigger Binding=IsUnknown_* Value=True → Foreground=Blue, FontWeight=Bold"
      pattern: "IsUnknown_"
---

<objective>
Replace the `MovementsView.xaml` stub with the full "Fichier d'édition" UI: import toolbar, display filters, main DataGrid with all required columns, unknown-cell blue highlighting via DataTrigger (D-03), "A ajouter" action badge (D-04), and a row context menu. Wire the DataContext to `EditionFileViewModel`.

Purpose: This is the final user-facing deliverable of the phase. After this plan, the complete Fichier d'édition workflow is usable.
Output: Updated `MovementsView.xaml` + `MovementsView.xaml.cs`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-fichier-edition/01-01-SUMMARY.md
@.planning/phases/01-fichier-edition/01-02-SUMMARY.md
@.planning/phases/01-fichier-edition/01-03-SUMMARY.md
</context>

<interfaces>
<!-- ViewModel surface from Plan 03 that XAML bindings must use EXACTLY -->

From AVCNDB.WPF/ViewModels/EditionFileViewModel.cs (Plan 03):
```csharp
// Collections
ObservableCollection<EditionRowViewModel> Rows   // filtered view
EditionRowViewModel? SelectedRow

// Import state
string CurrentFilePath
int TotalRowCount
int UnknownRowCount
int ApprovedRowCount

// Filter
string FilterType   // "Tous"|"Actifs"|"Inactifs"|"Affectés"|"Non-affectés"|
                    // "Méd.[V.E.I]"|"Avec A.P"|"Sans A.P"|"Remboursés"|"Non-remboursés"|
                    // "Sélectionnés"|"Non-sélectionnés"|"Changements de prix"

// Commands
IRelayCommand ImportCommand
IRelayCommand ApproveRowCommand
IRelayCommand RejectRowCommand
IRelayCommand MarkAsNewCommand
IRelayCommand MarkForDeletionCommand
IRelayCommand ResetRowCommand
IRelayCommand OpenLibraryManagerCommand
IRelayCommand SimilaritySearchCommand
IRelayCommand ExportCommand

// From ViewModelBase
bool IsLoading
string StatusMessage
```

From AVCNDB.WPF/ViewModels/EditionRowViewModel.cs (Plan 03):
```csharp
// Per-row DataGrid binding properties
int LineNumber
bool IsSelected      // two-way (checkbox column)
ActionFlag ActionFlag
string ActionLabel   // "A ajouter" | "Affecté" | "" etc.
bool HasAction
bool HasUnknownFields

string PctCode
string Denomination
string Dci
string DciAssociation
string Forme
string Tableau
string Veic
string Labo
decimal PrixRef
decimal Prix
bool IsRemboursable
bool IsAP
string Designation

// Per-field "IsUnknown" booleans for DataTrigger (D-03) — MUST use these exact names:
bool IsUnknown_Dci
bool IsUnknown_DciAssociation
bool IsUnknown_Labo
bool IsUnknown_Forme
bool IsUnknown_Fam1
bool IsUnknown_Fam2
bool IsUnknown_Fam3
bool IsUnknown_Voie
bool IsUnknown_Specialite
```

From AVCNDB.WPF/Models/EditionRow.cs (enums):
```csharp
public enum ActionFlag { None=0, AjouterNew=1, MarquerSuppression=2, Nouveau=3, Reinitialiser=4, Affecte=5, Desaffecte=6 }
```

Style brushes available from Styles/Colors.xaml:
```
PrimaryBrush, PrimaryDarkBrush, PrimaryLightBrush
SuccessBrush, SuccessLightBrush, SuccessDarkBrush
WarningBrush, WarningLightBrush, WarningDarkBrush
ErrorBrush,   ErrorLightBrush,   ErrorDarkBrush
InfoBrush,    InfoLightBrush,    InfoDarkBrush
BorderBrush
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild MovementsView.xaml — full Fichier d'édition UI</name>
  <files>AVCNDB.WPF/Views/MovementsView.xaml, AVCNDB.WPF/Views/MovementsView.xaml.cs</files>
  <action>
**COMPLETELY REPLACE** `MovementsView.xaml` and `MovementsView.xaml.cs`.

**MovementsView.xaml.cs** (replace entire file):

```csharp
using AVCNDB.WPF.ViewModels;

namespace AVCNDB.WPF.Views;

public partial class MovementsView : System.Windows.Controls.UserControl
{
    public MovementsView(EditionFileViewModel viewModel)
    {
        InitializeComponent();
        DataContext = viewModel;
    }
}
```

**MovementsView.xaml** (replace the ENTIRE current stub — it currently says "À venir"):

```xml
<UserControl x:Class="AVCNDB.WPF.Views.MovementsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:AVCNDB.WPF.ViewModels"
             xmlns:models="clr-namespace:AVCNDB.WPF.Models"
             xmlns:helpers="clr-namespace:AVCNDB.WPF.Helpers"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:EditionFileViewModel}"
             d:DesignHeight="800" d:DesignWidth="1400"
             Background="{DynamicResource MaterialDesignBackground}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <!-- Blue bold style for unknown field cells (D-03) -->
        <Style x:Key="UnknownCellTextBlockStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Style.Triggers>
                <!-- Activated by DataTrigger on the parent DataGridCell's DataContext -->
            </Style.Triggers>
        </Style>

        <!-- Action badge border style (D-04) -->
        <Style x:Key="ActionBadgeStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="6,2"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- Header -->
            <RowDefinition Height="Auto"/>  <!-- Import toolbar -->
            <RowDefinition Height="Auto"/>  <!-- Stats bar -->
            <RowDefinition Height="Auto"/>  <!-- Filter bar -->
            <RowDefinition Height="*"/>     <!-- DataGrid -->
            <RowDefinition Height="Auto"/>  <!-- Status bar -->
        </Grid.RowDefinitions>

        <!-- ════════════════════════════════════════
             HEADER
        ════════════════════════════════════════ -->
        <Border Grid.Row="0"
                Padding="24,14"
                Background="{DynamicResource MaterialDesignPaper}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Fichier d'édition"
                               Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                               Foreground="{StaticResource PrimaryBrush}"/>
                    <TextBlock Text="Import Excel · Détection automatique des données inconnues · Validation bibliothèque"
                               Style="{StaticResource MaterialDesignBody2TextBlock}"
                               Foreground="{DynamicResource MaterialDesignBodyLight}"
                               Margin="0,2,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Command="{Binding OpenLibraryManagerCommand}"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            Margin="0,0,8,0" Padding="10,6"
                            ToolTip="Gestionnaire de la bibliothèque">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="LibraryBooks" Width="16" Height="16" VerticalAlignment="Center"/>
                            <TextBlock Text="Bibliothèque" Margin="6,0,0,0" VerticalAlignment="Center" FontSize="12"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding ExportCommand}"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            Padding="10,6"
                            ToolTip="Exporter le fichier d'édition">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="FileExcel" Width="16" Height="16" VerticalAlignment="Center"/>
                            <TextBlock Text="Exporter" Margin="6,0,0,0" VerticalAlignment="Center" FontSize="12"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ════════════════════════════════════════
             IMPORT TOOLBAR
        ════════════════════════════════════════ -->
        <Border Grid.Row="1"
                Padding="24,10"
                Background="{DynamicResource MaterialDesignPaper}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">

                <TextBlock Text="Source :" VerticalAlignment="Center"
                           FontWeight="Medium" Margin="0,0,10,0" FontSize="13"/>

                <ComboBox Width="220" Height="36"
                          SelectedIndex="0"
                          Style="{StaticResource MaterialDesignOutlinedComboBox}"
                          materialDesign:HintAssist.Hint="Type de source"
                          VerticalContentAlignment="Center"
                          Margin="0,0,12,0">
                    <ComboBoxItem Content="Table Excel simple"/>
                    <ComboBoxItem Content="Fichier medic AVICENNA / DBF"/>
                    <ComboBoxItem Content="VIDAL TN / DBF"/>
                    <ComboBoxItem Content="CNAM / XLS"/>
                </ComboBox>

                <Button Command="{Binding ImportCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Background="{StaticResource PrimaryBrush}"
                        Foreground="White"
                        BorderBrush="{StaticResource PrimaryBrush}"
                        Padding="16,8" Height="36"
                        Margin="0,0,20,0">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="FolderOpen" Width="16" Height="16" VerticalAlignment="Center"/>
                        <TextBlock Text="Créer un nouveau fichier d'édition" Margin="8,0,0,0" VerticalAlignment="Center" FontSize="13"/>
                    </StackPanel>
                </Button>

                <!-- Current file path indicator -->
                <TextBlock VerticalAlignment="Center"
                           FontSize="12"
                           Foreground="{DynamicResource MaterialDesignBodyLight}"
                           Text="{Binding CurrentFilePath}"
                           TextTrimming="CharacterEllipsis"
                           MaxWidth="300"
                           ToolTip="{Binding CurrentFilePath}"/>

                <!-- Loading spinner -->
                <ProgressBar IsIndeterminate="True"
                             Width="20" Height="20"
                             Style="{StaticResource MaterialDesignCircularProgressBar}"
                             Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"
                             Margin="12,0,0,0"
                             VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- ════════════════════════════════════════
             STATS BAR (total / unknown / approved)
        ════════════════════════════════════════ -->
        <Border Grid.Row="2"
                Padding="24,8"
                Background="{DynamicResource MaterialDesignPaper}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">

                <Border Background="{StaticResource InfoLightBrush}"
                        CornerRadius="4" Padding="12,6" Margin="0,0,10,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Total :" FontSize="12" FontWeight="Medium"
                                   Foreground="{StaticResource InfoDarkBrush}" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding TotalRowCount}" FontSize="14" FontWeight="Bold"
                                   Foreground="{StaticResource InfoDarkBrush}" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <Border Background="{StaticResource WarningLightBrush}"
                        CornerRadius="4" Padding="12,6" Margin="0,0,10,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Inconnues :" FontSize="12" FontWeight="Medium"
                                   Foreground="{StaticResource WarningDarkBrush}" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding UnknownRowCount}" FontSize="14" FontWeight="Bold"
                                   Foreground="{StaticResource WarningDarkBrush}" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <Border Background="{StaticResource SuccessLightBrush}"
                        CornerRadius="4" Padding="12,6">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Approuvées :" FontSize="12" FontWeight="Medium"
                                   Foreground="{StaticResource SuccessDarkBrush}" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding ApprovedRowCount}" FontSize="14" FontWeight="Bold"
                                   Foreground="{StaticResource SuccessDarkBrush}" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>

        <!-- ════════════════════════════════════════
             DISPLAY FILTER BAR (13 filters)
        ════════════════════════════════════════ -->
        <Border Grid.Row="3"
                Padding="24,6"
                Background="{DynamicResource MaterialDesignPaper}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,0,0,1">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                <WrapPanel Orientation="Horizontal">
                    <!-- Use ToggleButton style tabs for the display filters -->
                    <!-- Each sets FilterType to its string value via Click handler in VM -->
                    <!-- Simplest approach: RadioButton group bound to FilterType string -->

                    <RadioButton Content="Tous"
                                 Tag="Tous"
                                 IsChecked="{Binding FilterType, Converter={x:Static materialDesign:StringToEnumConverter.Instance}, ConverterParameter=Tous, Mode=TwoWay, FallbackValue=True}"
                                 Style="{StaticResource MaterialDesignTabRadioButton}"
                                 Margin="0,0,4,4"/>
                    <RadioButton Content="Actifs"
                                 Tag="Actifs"
                                 Style="{StaticResource MaterialDesignTabRadioButton}"
                                 Margin="0,0,4,4"
                                 Click="FilterButton_Click"/>
                    <RadioButton Content="Inactifs"
                                 Tag="Inactifs"
                                 Style="{StaticResource MaterialDesignTabRadioButton}"
                                 Margin="0,0,4,4"
                                 Click="FilterButton_Click"/>
                    <RadioButton Content="Affectés"
                                 Tag="Affectés"
                                 Style="{StaticResource MaterialDesignTabRadioButton}"
                                 Margin="0,0,4,4"
                                 Click="FilterButton_Click"/>
                    <RadioButton Content="Non-affectés"
                                 Tag="Non-affectés"
                                 Style="{StaticResource MaterialDesignTabRadioButton}"
                                 Margin="0,0,4,4"
                                 Click="FilterButton_Click"/>
                    <RadioButton Content="Méd.[V.E.I]"
                                 Tag="Méd.[V.E.I]"
                                 Style="{StaticResource MaterialDesignTabRadioButton}"
                                 Margin="0,0,4,4"
                                 Click="FilterButton_Click"/>
                    <RadioButton Content="Avec A.P"
                                 Tag="Avec A.P"
                                 Style="{StaticResource MaterialDesignTabRadioButton}"
                                 Margin="0,0,4,4"
                                 Click="FilterButton_Click"/>
                    <RadioButton Content="Sans A.P"
                                 Tag="Sans A.P"
                                 Style="{StaticResource MaterialDesignTabRadioButton}"
                                 Margin="0,0,4,4"
                                 Click="FilterButton_Click"/>
                    <RadioButton Content="Remboursés"
                                 Tag="Remboursés"
                                 Style="{StaticResource MaterialDesignTabRadioButton}"
                                 Margin="0,0,4,4"
                                 Click="FilterButton_Click"/>
                    <RadioButton Content="Non-remboursés"
                                 Tag="Non-remboursés"
                                 Style="{StaticResource MaterialDesignTabRadioButton}"
                                 Margin="0,0,4,4"
                                 Click="FilterButton_Click"/>
                    <RadioButton Content="Sélectionnés"
                                 Tag="Sélectionnés"
                                 Style="{StaticResource MaterialDesignTabRadioButton}"
                                 Margin="0,0,4,4"
                                 Click="FilterButton_Click"/>
                    <RadioButton Content="Non-sélectionnés"
                                 Tag="Non-sélectionnés"
                                 Style="{StaticResource MaterialDesignTabRadioButton}"
                                 Margin="0,0,4,4"
                                 Click="FilterButton_Click"/>
                    <RadioButton Content="Changements de prix"
                                 Tag="Changements de prix"
                                 Style="{StaticResource MaterialDesignTabRadioButton}"
                                 Margin="0,0,4,4"
                                 Click="FilterButton_Click"/>
                </WrapPanel>
            </ScrollViewer>
        </Border>

        <!-- ════════════════════════════════════════
             MAIN DATAGRID
        ════════════════════════════════════════ -->
        <Grid Grid.Row="4">
            <DataGrid ItemsSource="{Binding Rows}"
                      SelectedItem="{Binding SelectedRow}"
                      Style="{StaticResource ModernDataGridStyle}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      SelectionMode="Single"
                      Margin="0">

                <!-- Row context menu (D-04 actions) -->
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow" BasedOn="{StaticResource MaterialDesignDataGridRow}">
                        <Setter Property="ContextMenu">
                            <Setter.Value>
                                <ContextMenu>
                                    <MenuItem Header="Substituer"
                                              Command="{Binding PlacementTarget.Tag.SimilaritySearchCommand,
                                                        RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding}"/>
                                    <MenuItem Header="Désaffecter"
                                              Command="{Binding PlacementTarget.Tag.RejectRowCommand,
                                                        RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding}"/>
                                    <Separator/>
                                    <MenuItem Header="Marquer comme nouveau"
                                              Command="{Binding PlacementTarget.Tag.MarkAsNewCommand,
                                                        RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding}"/>
                                    <MenuItem Header="Marquer pour la suppression"
                                              Command="{Binding PlacementTarget.Tag.MarkForDeletionCommand,
                                                        RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding}"/>
                                    <MenuItem Header="Ré-initialiser"
                                              Command="{Binding PlacementTarget.Tag.ResetRowCommand,
                                                        RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding}"/>
                                    <Separator/>
                                    <MenuItem Header="Approuver (ajouter à la bibliothèque)"
                                              Command="{Binding PlacementTarget.Tag.ApproveRowCommand,
                                                        RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding}"/>
                                </ContextMenu>
                            </Setter.Value>
                        </Setter>
                        <!-- Tag the row with the DataContext (ViewModel) so ContextMenu can reach commands -->
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasUnknownFields}" Value="True">
                                <Setter Property="Background" Value="#FFF5F5FF"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.Tag>
                    <!-- Bind DataGrid.Tag to the ViewModel so ContextMenu RelativeSource works -->
                    <Binding Path="DataContext" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                </DataGrid.Tag>

                <DataGrid.Columns>

                    <!-- ☑ (checkbox) -->
                    <DataGridCheckBoxColumn Header="☑"
                                            Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}"
                                            Width="36"
                                            IsReadOnly="False"/>

                    <!-- Lig. (line number) -->
                    <DataGridTextColumn Header="Lig."
                                        Binding="{Binding LineNumber}"
                                        Width="46" IsReadOnly="True"/>

                    <!-- Id. (existing medic id) -->
                    <DataGridTextColumn Header="Id."
                                        Binding="{Binding Row.ExistingMedicId}"
                                        Width="50" IsReadOnly="True"/>

                    <!-- Action (badge column — D-04) -->
                    <DataGridTemplateColumn Header="Action" Width="110" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource ActionBadgeStyle}">
                                    <Border.Style>
                                        <Style TargetType="Border" BasedOn="{StaticResource ActionBadgeStyle}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ActionFlag}" Value="{x:Static models:ActionFlag.AjouterNew}">
                                                    <Setter Property="Background" Value="{StaticResource InfoLightBrush}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ActionFlag}" Value="{x:Static models:ActionFlag.MarquerSuppression}">
                                                    <Setter Property="Background" Value="{StaticResource ErrorLightBrush}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ActionFlag}" Value="{x:Static models:ActionFlag.Nouveau}">
                                                    <Setter Property="Background" Value="{StaticResource SuccessLightBrush}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ActionFlag}" Value="{x:Static models:ActionFlag.Affecte}">
                                                    <Setter Property="Background" Value="{StaticResource SuccessLightBrush}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding ActionLabel}" FontSize="11" FontWeight="SemiBold"
                                               HorizontalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ActionFlag}" Value="{x:Static models:ActionFlag.AjouterNew}">
                                                        <Setter Property="Foreground" Value="{StaticResource InfoDarkBrush}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ActionFlag}" Value="{x:Static models:ActionFlag.MarquerSuppression}">
                                                        <Setter Property="Foreground" Value="{StaticResource ErrorDarkBrush}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ActionFlag}" Value="{x:Static models:ActionFlag.Nouveau}">
                                                        <Setter Property="Foreground" Value="{StaticResource SuccessDarkBrush}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ActionFlag}" Value="{x:Static models:ActionFlag.Affecte}">
                                                        <Setter Property="Foreground" Value="{StaticResource SuccessDarkBrush}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Code PCT -->
                    <DataGridTextColumn Header="Code PCT" Binding="{Binding PctCode}"
                                        Width="90" IsReadOnly="True"/>

                    <!-- Dénomination -->
                    <DataGridTextColumn Header="Dénomination" Binding="{Binding Denomination}"
                                        Width="*" MinWidth="160" IsReadOnly="True"/>

                    <!-- D.C.J (DCI) — unknown cells blue+bold (D-03) -->
                    <DataGridTemplateColumn Header="D.C.J" Width="120" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Dci}">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
                                            <Setter Property="FontWeight" Value="Normal"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsUnknown_Dci}" Value="True">
                                                    <Setter Property="Foreground" Value="#1565C0"/>
                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Association (DCI 2/3/4) — unknown cells blue+bold -->
                    <DataGridTemplateColumn Header="Association" Width="120" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding DciAssociation}">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
                                            <Setter Property="FontWeight" Value="Normal"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsUnknown_DciAssociation}" Value="True">
                                                    <Setter Property="Foreground" Value="#1565C0"/>
                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Forme — unknown cells blue+bold -->
                    <DataGridTemplateColumn Header="Forme" Width="90" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Forme}">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
                                            <Setter Property="FontWeight" Value="Normal"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsUnknown_Forme}" Value="True">
                                                    <Setter Property="Foreground" Value="#1565C0"/>
                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Tab. -->
                    <DataGridTextColumn Header="Tab." Binding="{Binding Tableau}"
                                        Width="60" IsReadOnly="True"/>

                    <!-- VEiC -->
                    <DataGridTextColumn Header="VEiC" Binding="{Binding Veic}"
                                        Width="60" IsReadOnly="True"/>

                    <!-- Labo. — unknown cells blue+bold -->
                    <DataGridTemplateColumn Header="Labo." Width="100" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Labo}">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
                                            <Setter Property="FontWeight" Value="Normal"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsUnknown_Labo}" Value="True">
                                                    <Setter Property="Foreground" Value="#1565C0"/>
                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Prix-Réf -->
                    <DataGridTextColumn Header="Prix-Réf"
                                        Binding="{Binding PrixRef, StringFormat=N2}"
                                        Width="75" IsReadOnly="True"/>

                    <!-- Prix -->
                    <DataGridTextColumn Header="Prix"
                                        Binding="{Binding Prix, StringFormat=N2}"
                                        Width="75" IsReadOnly="True"/>

                    <!-- Remb. -->
                    <DataGridCheckBoxColumn Header="Remb."
                                            Binding="{Binding IsRemboursable}"
                                            Width="55" IsReadOnly="True"/>

                    <!-- A.P. -->
                    <DataGridCheckBoxColumn Header="A.P."
                                            Binding="{Binding IsAP}"
                                            Width="45" IsReadOnly="True"/>

                    <!-- Désignation -->
                    <DataGridTextColumn Header="Désignation"
                                        Binding="{Binding Designation}"
                                        Width="140" IsReadOnly="True"/>

                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- ════════════════════════════════════════
             STATUS BAR
        ════════════════════════════════════════ -->
        <Border Grid.Row="5"
                Padding="24,6"
                Background="{DynamicResource MaterialDesignPaper}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,1,0,0">
            <TextBlock Text="{Binding StatusMessage}"
                       FontSize="12"
                       Foreground="{DynamicResource MaterialDesignBodyLight}"/>
        </Border>

    </Grid>
</UserControl>
```

**MovementsView.xaml.cs** — replace entire file AND add the `FilterButton_Click` code-behind handler:

```csharp
using System.Windows;
using System.Windows.Controls;
using AVCNDB.WPF.ViewModels;

namespace AVCNDB.WPF.Views;

public partial class MovementsView : UserControl
{
    public MovementsView(EditionFileViewModel viewModel)
    {
        InitializeComponent();
        DataContext = viewModel;
    }

    /// <summary>
    /// Filter RadioButton click handler — sets ViewModel.FilterType from the button's Tag.
    /// Used because RadioButton IsChecked binding to a string property is complex without a converter.
    /// </summary>
    private void FilterButton_Click(object sender, RoutedEventArgs e)
    {
        if (sender is RadioButton rb && DataContext is EditionFileViewModel vm)
        {
            vm.FilterType = rb.Tag?.ToString() ?? "Tous";
        }
    }
}
```

**Implementation notes for the executor:**

1. **StringToEnumConverter for "Tous" filter tab**: The first RadioButton uses `StringToEnumConverter` which will not work for a plain `string` property. Replace the IsChecked binding on the "Tous" RadioButton with `Click="FilterButton_Click"` like the others, so ALL filter tabs use the code-behind click handler.

2. **ContextMenu RelativeSource**: The context menu command binding pattern using `PlacementTarget.Tag` requires that the DataGrid's Tag be bound to the UserControl's DataContext (ViewModel). The XAML above uses `<DataGrid.Tag><Binding Path="DataContext" RelativeSource=.../>`. This is a common WPF pattern — verify it works, and if not, use a `helpers:BindingProxy` instead (the project already has this helper).

3. **`ModernDataGridStyle`**: Use exactly this key (it exists in DataGridStyles.xaml). Do NOT define a new DataGrid style.

4. **ActionFlag enum in XAML**: The `x:Static models:ActionFlag.AjouterNew` binding requires the `xmlns:models="clr-namespace:AVCNDB.WPF.Models"` namespace (already declared at the top of the UserControl).

5. **Blue color `#1565C0`**: This is Material Design Blue 800. It matches the "blue bold for unknown data" decision (D-03). Do not replace it with a DynamicResource — the explicit hex value is intentional.
  </action>
  <verify>
    <automated>dotnet build AVCNDB.WPF/AVCNDB.WPF.csproj --no-restore 2>&1 | tail -15</automated>
  </verify>
  <done>
  - `AVCNDB.WPF.csproj` builds with 0 errors
  - `MovementsView.xaml` no longer contains "À venir" or "Mouvements de stock"
  - DataContext is wired to `EditionFileViewModel` via constructor injection
  - DataGrid ItemsSource binds to `Rows`
  - At least 4 DataTriggers present for `IsUnknown_*` properties (per D-03)
  - Action column template present with ActionFlag DataTriggers (per D-04)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 01 complete — the full "Fichier d'édition" workflow:
    - EditionRow model + EditionFileSession entity + service contracts (Plan 01)
    - FuzzyDetectionService (FuzzySharp ML) + EditionFileService + unit tests (Plan 02)
    - EditionFileViewModel + EditionRowViewModel + DI wiring + ViewModel tests (Plan 03)
    - MovementsView.xaml full Fichier d'édition UI — toolbar, DataGrid, filters, blue cells, context menu (Plan 04)
  </what-built>
  <how-to-verify>
    1. Build: `dotnet build AVCNDB.WPF/AVCNDB.WPF.csproj` → must be 0 errors
    2. Run: `dotnet run --project AVCNDB.WPF`
    3. Navigate to **Mouvements** in the sidebar (it should now say "Fichier d'édition")
    4. Verify the page header shows "Fichier d'édition" (not the old placeholder)
    5. Verify the import toolbar shows "Source:", a ComboBox with 4 options, and "Créer un nouveau fichier d'édition" button
    6. Verify the filter bar shows all 13 filter options (Tous, Actifs, Inactifs, Affectés, etc.)
    7. Verify the DataGrid shows all required columns: ☑ Lig. Id. Action Code-PCT Dénomination D.C.J Association Forme Tab. VEiC Labo. Prix-Réf Prix Remb. A.P. Désignation
    8. Click "Créer un nouveau fichier d'édition" → an Open File dialog appears (no crash)
    9. If you have a test Excel file: import it → rows appear in the grid
    10. If any rows have unknown data: their Labo/DCI/Forme cells should appear blue+bold
    11. Right-click a row → context menu appears with: Substituer, Désaffecter, Marquer comme nouveau, Marquer pour la suppression, Ré-initialiser, Approuver
    12. Click "Bibliothèque" button → info dialog appears
    13. Run tests: `dotnet test AVCNDB.WPF.Tests` → all tests pass
  </how-to-verify>
  <resume-signal>Type "approved" if the Fichier d'édition page works correctly, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
- `dotnet build AVCNDB.WPF/AVCNDB.WPF.csproj` → 0 errors
- `dotnet test AVCNDB.WPF.Tests` → all tests pass
- MovementsView.xaml does NOT contain "À venir" or "SwapHorizontal"
- DataGrid ItemsSource="{Binding Rows}" is present
- At least one `IsUnknown_Dci` DataTrigger present with Foreground="#1565C0"
- ActionFlag enum values referenced via x:Static in DataTriggers
- code-behind constructor accepts `EditionFileViewModel`
</verification>

<success_criteria>
- Fichier d'édition page is fully functional
- Unknown cells highlighted blue+bold (D-03)
- "A ajouter" badge visible on rows with unknowns (D-04)
- All 13 display filters work
- Context menu provides all 6 row actions
- Import flow: pick file → parse Excel → ML detection → grid populated
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-fichier-edition/01-04-SUMMARY.md` summarizing:
- XAML structure (sections implemented)
- Any deviations from the XAML spec (e.g., ContextMenu binding approach used)
- How blue cell highlighting was implemented (DataTrigger approach)
- Human verification result
- Final test count and results
</output>
